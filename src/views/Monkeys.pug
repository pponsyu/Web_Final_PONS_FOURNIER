extends layout.pug

block entete
    h1 All Monkeys :
    ul
        each Monkey in Monk
            h2
                - var URL = '/Find_Monkey/' + Monkey.id
                a(class='button' href= URL) #{Monkey.Name}
               

block content
    h2
        .row
            .col-md-6
                a(href='/MonkeyCreation') Add a new Monkey
    h2
        .row
            .col-md-6
                 a(href='/') Return